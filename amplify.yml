version: 1
frontend:
  phases:
    build:
      commands:
        - echo "✅ 準備部署 HTML 檔案..."
        - ls -la src/
        - echo "🎮 找到以下遊戲檔案："
        - find src -name "*.html" -type f | sed 's/^/   /'
  artifacts:
    baseDirectory: src
    files:
      - '**/*'
  cache:
    paths: [] 